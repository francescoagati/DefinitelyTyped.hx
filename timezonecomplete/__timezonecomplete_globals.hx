extern class __timezonecomplete_globalsTopLevel {
	static function min(d1:DateTime, d2:DateTime):DateTime;
	static function min(d1:Duration, d2:Duration):Duration;
	static function max(d1:DateTime, d2:DateTime):DateTime;
	static function max(d1:Duration, d2:Duration):Duration;
	static function abs(d:Duration):Duration;
}
