extern class Serve-faviconTopLevel {
	static function serveFavicon(path:String, ?options:{ @:optional
	var maxAge : Float; }):express.RequestHandler;
}
extern class Serve-faviconTopLevel {
	static function serveFavicon(path:String, ?options:{ @:optional
	var maxAge : Float; }):express.RequestHandler;
}
