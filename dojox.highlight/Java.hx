typedef DefaultMode = {
	var contains : Array<Dynamic>;
	var illegal : String;
	var keywords : Dynamic;
	var lexems : Array<Dynamic>;
};
typedef Javascript = {
	var defaultMode : Dynamic;
	var modes : Array<Dynamic>;
};
