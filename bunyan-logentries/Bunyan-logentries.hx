typedef StreamOptions = {
	var token : String;
};
extern class Bunyan-logentriesTopLevel {
	static function createStream(options:StreamOptions):NodeJS.WritableStream;
}
extern class Bunyan-logentriesTopLevel {
	static function createStream(options:StreamOptions):NodeJS.WritableStream;
}
