extern class Express-unlessTopLevel {
	static function unless(options:unless.Options):express.RequestHandler;
}
extern class Express-unlessTopLevel {
	static function unless(options:unless.Options):express.RequestHandler;
}
