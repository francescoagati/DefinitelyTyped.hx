extern class Readdir-streamTopLevel {
	static function readdir(dir:String):NodeJS.ReadableStream;
}
extern class Readdir-streamTopLevel {
	static function readdir(dir:String):NodeJS.ReadableStream;
}
